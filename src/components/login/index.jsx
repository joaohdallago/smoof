import AuthContainer from '../../theme/AuthContainer';

import Logo from '../logo';

export default function Login() {
	return (
		<AuthContainer>
			<Logo />
			<h1>LOGIN</h1>
		</AuthContainer>
	);
}